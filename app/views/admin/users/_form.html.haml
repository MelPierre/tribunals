= simple_form_for @user, url: admin_user_path(params[:tribunal_code], @user) do |f|
  .form-inputs
    = f.input :name
    = f.input :email
    = f.input :admin
    = f.association :tribunals
  .form-actions
    = f.button :submit, 'Save'